Project:
A Next.js app that connects to OpenAI's API to summarize PDF documents.
Converta is prototype that uploads and summarizes PDF documents using Next.js and the OpenAI API

steps
After setting up nextjs
npm install openai unpdf

usingRAG
npm install @lancedb/lancedb langchain @langchain/community --legacy-peer-deps, used --legacy-peers-deps to resolve the dependency tree version conflict caused because LangChain is strict about checking the exact version numbers of my installed packages (like openai)
ollama pull nomic-embed-text in powershell terminal

